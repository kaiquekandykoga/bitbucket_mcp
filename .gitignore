# Environment
.env
.env.local
.env.*.local

# Bundler / build artifacts
# Gemfile.lock is intentionally not committed: this is a gem, so CI resolves
# dependencies fresh against each supported Ruby version.
/Gemfile.lock
/.bundle/
/vendor/bundle/
/pkg/
/*.gem

# Test & tooling caches
/coverage/
/.rubocop-cache/

# IDE / Editor
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Claude Code local settings
.claude/settings.local.json
